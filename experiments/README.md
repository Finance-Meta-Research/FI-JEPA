# Experiments

This folder stores benchmark outputs generated by the repo.

- `results_synthetic.json`: single-run synthetic sanity benchmark
- `results_macrodata.json`: single-run macrodata benchmark
- `results_suite_macro.json`: multi-seed ablation suite over macrodata

Each JSON file contains the full run history, probe metrics, baseline metrics, and latent statistics.
