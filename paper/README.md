# FI-JEPA paper

Compile `main.tex` directly. The style file `arxivrefined.sty` is included in this folder.

A helper script can regenerate the paper figures from the saved experiment JSON:

```bash
python ../scripts/make_paper_assets.py
```
